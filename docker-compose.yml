version: '3.4'
services:
  ssv.avado.dappnode.eth:
    build:
      context: build
      args:
        UPSTREAM_VERSION: v1.3.6
    image: 'ssv.avado.dappnode.eth:0.0.12'
    restart: always
    volumes:
      - 'ssv:/data'
    ports:
      - '13001:13001'
      - '12001:12001/udp'
      - '9999:9999'
    environment:
      - 'BEACONNODEADDR=http://lighthouse-holesky.my.ava.do:5052'
      - 'EXECUTIONCLIENTADDR=ws://nethermind-holesky.my.ava.do:8545'
      - NETWORK=holesky
volumes:
  ssv: {}
